# Concrete Strength Predictor

A simple Streamlit app for predicting concrete compressive strength from mix values.

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
```

## Notes

The app uses the saved model file in this folder and keeps the interface plain and lightweight.
